'use client';

import React from 'react';
// import { UserType } from '../FetchComponent';

const RefetchButton = ({}: // refetchProp,
{
  // refetchProp: Promise<UserType | undefined>;
}) => {
  return <div>{/* <button onClick={refetchProp}>fetch some </button> */}</div>;
};
